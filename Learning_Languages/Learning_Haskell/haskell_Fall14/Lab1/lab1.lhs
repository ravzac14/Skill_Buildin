Zack Raver; ravzac14
9/30/14

This is from the Variables and functions section of Haskell wiki thinger


This was the second part of the lab, the rest was do the homework...so this is the part I'm submitting as lab


>	r = 5

>	area rad = pi * rad ^ 2

>	areaRect l w = l * w

>	areaTri b h = (b * h) / 2

>	quadruple x = double (double x)
>		where
>			double y = y + y

>	subtract x y = x - y

>	xor p q = (p || q) && not (p && q)
